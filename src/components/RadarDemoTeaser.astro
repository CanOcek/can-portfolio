<article class="radar-demo-teaser">
  <figure class="radar-demo-teaser__media">
    <img
      src="/images/projects/radar-extracted/radar-pdf-obj-320.png"
      alt="Streamlit Business Development Radar dashboard showing pre-generated key takeaways."
    />
  </figure>
  <div class="radar-demo-teaser__body">
    <p class="radar-demo-teaser__eyebrow">Interactive demo</p>
    <h3>Open the Radar demo</h3>
    <p>
      Launch the native Streamlit interface inside the portfolio to preview the original retrieval
      and synthesis workflow.
    </p>
    <div class="radar-demo-teaser__actions">
      <a class="radar-demo-teaser__button" href="/work/web-intelligence-rag-pipeline/streamlit/">Native Streamlit</a>
    </div>
  </div>
</article>

<style>
  .radar-demo-teaser {
    background:
      linear-gradient(180deg, rgb(255 255 255 / 0.045), transparent),
      var(--color-surface);
    border: var(--border-subtle);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-soft);
    color: var(--color-text);
    display: grid;
    gap: 0;
    grid-template-columns: minmax(0, 1.15fr) minmax(18rem, 0.85fr);
    overflow: hidden;
  }

  .radar-demo-teaser__media {
    background: var(--color-bg);
    margin: 0;
    min-height: 100%;
  }

  .radar-demo-teaser__media img {
    aspect-ratio: 16 / 10;
    block-size: 100%;
    inline-size: 100%;
    object-fit: cover;
    object-position: top center;
  }

  .radar-demo-teaser__body {
    align-content: center;
    border-inline-start: var(--border-subtle);
    display: grid;
    gap: var(--space-3);
    padding: var(--space-5);
  }

  .radar-demo-teaser__body h3,
  .radar-demo-teaser__body p {
    margin: 0;
  }

  .radar-demo-teaser__body h3 {
    font-size: 1.8rem;
    letter-spacing: 0;
  }

  .radar-demo-teaser__eyebrow {
    color: var(--color-cool);
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  .radar-demo-teaser__button {
    align-items: center;
    background: linear-gradient(135deg, var(--color-accent), var(--color-accent-strong));
    border-radius: var(--radius-sm);
    color: var(--color-bg);
    display: inline-flex;
    font-weight: 700;
    justify-content: center;
    min-height: 2.75rem;
    padding: 0.7rem 1rem;
    text-decoration: none;
    transition:
      border-color var(--transition-fast),
      transform var(--transition-fast);
  }

  .radar-demo-teaser__button:hover,
  .radar-demo-teaser__button:focus-visible {
    color: var(--color-bg);
    transform: translateY(-2px);
  }

  .radar-demo-teaser__actions {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-block-start: var(--space-2);
  }

  @media (max-width: 820px) {
    .radar-demo-teaser {
      grid-template-columns: 1fr;
    }

    .radar-demo-teaser__body {
      border-block-start: var(--border-subtle);
      border-inline-start: 0;
    }
  }
</style>
